/**
 * Global Loading State
 *
 * Automatically rendered by Next.js while a page route is loading.
 * Currently returns null to avoid flashing a spinner for fast transitions,
 * but can be customized for a global skeleton loader.
 */
export default function Loading() {
  return null
}
